#pragma once

#ifndef MULANSTR_BACKEND_DUMMY_HPP
#define MULANSTR_BACKEND_DUMMY_HPP

/**
 * Backend for an interface that returns the translation string back.
*/

#include "../backend.hpp"

namespace mls::backend {
	
	///empty init function
	void init();
	
}

#endif /* MULANSTR_BACKEND_DUMMY_HPP */
//file end
